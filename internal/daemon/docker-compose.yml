services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: agent-registry-postgres
    environment:
      POSTGRES_DB: agentregistry
      POSTGRES_USER: agentregistry
      POSTGRES_PASSWORD: agentregistry
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agentregistry -d agentregistry"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - agentregistry-network

  agentregistry:
    image: ${DOCKER_REGISTRY}/agentregistry-dev/agentregistry/server:${VERSION}
    container_name: agentregistry-server
    # Rewrite the kubeconfig so that 127.0.0.1/localhost server addresses resolve
    # to the Docker host. This is required for local k8s clusters (kind, minikube,
    # Docker Desktop) where the API server listens on the host machine.
    entrypoint:
      - /bin/sh
      - -c
      - |
        if [ -f /root/.kube/config.orig ]; then
          mkdir -p /root/.kube
          sed -E \
            -e 's|https://127\.0\.0\.1|https://host.docker.internal|g' \
            -e 's|https://localhost|https://host.docker.internal|g' \
            -e 's|certificate-authority-data:.*|insecure-skip-tls-verify: true|g' \
            /root/.kube/config.orig > /root/.kube/config
        fi
        exec /app/bin/arctl-server
    environment:
      AGENT_REGISTRY_DATABASE_URL: "postgres://agentregistry:agentregistry@postgres:5432/agentregistry?sslmode=disable"
      AGENT_REGISTRY_SERVER_ADDRESS: ":8080"
      AGENT_REGISTRY_AGENT_GATEWAY_PORT: "21212"
      AGENT_REGISTRY_JWT_PRIVATE_KEY: ""
      AGENT_REGISTRY_BOOTSTRAP_ADMIN_USERNAME: "admin"
      AGENT_REGISTRY_BOOTSTRAP_ADMIN_PASSWORD: "admin"
      AGENT_REGISTRY_PUBLIC_ACTIONS: ""
      # Temporarily only for local development
      AGENT_REGISTRY_ENABLE_REGISTRY_VALIDATION: "false"
      AGENT_REGISTRY_PLATFORM_MODE: "docker"
      AGENT_REGISTRY_STORAGE_DIR: "/var/lib/agentregistry/storage"
      AGENT_REGISTRY_MCP_PORT: "31313"
      KUBECONFIG: "/root/.kube/config"
      # AGENT_REGISTRY_ENABLE_REGISTRY_VALIDATION: "true"
    ports:
      - "12121:8080"
      - "31313:31313"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      # Mount Docker socket to allow the server to manage containers
      - /var/run/docker.sock:/var/run/docker.sock
      # Mount /tmp so the registry can write configs accessible to Docker on the host
      - /tmp:/tmp
      # Persist registry-hosted SHUB packages on the host for easier inspection and backup
      - ${HOME}/Documents/skill-storage:/var/lib/agentregistry/storage
      # Mount kubeconfig as read-only source; the entrypoint rewrites it for Docker networking
      - ~/.kube/config:/root/.kube/config.orig:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - agentregistry-network
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local

networks:
  agentregistry-network:
    driver: bridge
